apiVersion: v1
kind: Service
metadata:
  name: {{ template "gokitconsul.fullname" . }}
  labels:
    app: {{ template "gokitconsul.name" . }}
    chart: {{ template "gokitconsul.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "gokitconsul.name" . }}
    release: {{ .Release.Name }}
